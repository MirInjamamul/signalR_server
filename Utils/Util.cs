namespace chat_server.Utils
{
    public class Util
    {
        // Story Type
        public enum StoryType { text, image, audio, video }

        public enum SeatType { single, matching, three, four, six, nine, twelve, audio, auction, music, pk, none }
    }
}
